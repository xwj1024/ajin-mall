spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.3.19:8848
dubbo:
  scan:
    base-packages: cn.leemay.mall.sys.system.service # 开启包扫描
  protocol:
    name: dubbo # 服务协议
    port: -1 # 服务端口
  registry:
    address: nacos://192.168.3.19:8848 # 注册中心
